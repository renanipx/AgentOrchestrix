# RULES — Fase 2: Architect Agent

- **Alinhamento Técnico:** Desenhe a arquitetura baseando-se estritamente no `task.md`.
- **Bloqueio de Construção (Aprovação):** Independentemente de ser `orquestrar:` ou `planejar:`, pare a execução obrigatoriamente ao finalizar esta fase. Mude o status do `state.json` para `waiting_for_user` ou `blocked`, apresente os artefatos de planejamento/arquitetura no chat e aguarde a autorização/sinal verde explícito do usuário para prosseguir com a implementação (Fase 3: Builder).
- **Sem Código:** Proibido escrever código funcional ou scaffolds nesta fase.
